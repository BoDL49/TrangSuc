@model IEnumerable<WebTrangSuc.Models.MauSac>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}



<h2>Danh sách Màu Sắc</h2>
<a href="@Url.Action("Create", "Color", new { area = "Admin" })" class="btn btn-success">Thêm mới</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Tên Màu</th>
            <th>Hành Động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ID</td>
                <td>@item.TenMau</td>
                <td>
                    <a href="@Url.Action("Edit", "Color", new { id = item.ID, area = "Admin" })" class="btn btn-warning">Sửa</a>
                    <a href="@Url.Action("Delete", "Color", new { id = item.ID, area = "Admin" })" class="btn btn-danger">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>


